/**
 * Feature flags for enabling/disabling features across the app.
 * Toggle these flags to enable or disable features without removing code.
 */
export const FEATURES = {
	/** Set to true to enable the live streaming feature */
	OSHI_LIVE_ENABLED: false
} as const;
